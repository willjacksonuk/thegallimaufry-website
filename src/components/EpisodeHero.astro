---
import { durationToReadable } from "../lib/buzzsprout";
import { dateToReadable } from "../lib/buzzsprout";
import type { Episode } from "../types/episode";
const { episode } = Astro.props as { episode: Episode };
---

<section>
  <div class="hero">
    <div class="hero-outer fade-up-text">
      <div class="hero-inner">
        <div class="hero-content">
          <p class="hero-eyebrow">
            Episode {episode.episode_number} - {
              dateToReadable(episode.published_at)
            }
          </p>
          <h1 class="episode-hero-title">{episode.title}</h1>
          <p class="episode-hero-time">
            Length: {durationToReadable(episode.duration)}
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
